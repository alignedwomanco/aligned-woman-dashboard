{
  "name": "ToolRun",
  "type": "object",
  "properties": {
    "toolSlug": {
      "type": "string"
    },
    "status": {
      "type": "string",
      "enum": [
        "InProgress",
        "Complete"
      ],
      "default": "InProgress"
    },
    "completedAt": {
      "type": "string",
      "format": "date-time"
    },
    "outputs": {
      "type": "object"
    },
    "description": {
      "type": "string",
      "maxLength": 1000
    }
  },
  "required": [
    "toolSlug"
  ],
  "rls": {
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "read": {
      "created_by_id": "{{user.id}}"
    },
    "update": {
      "created_by_id": "{{user.id}}"
    },
    "delete": {
      "created_by_id": "{{user.id}}"
    }
  }
}