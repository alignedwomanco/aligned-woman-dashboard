{
  "name": "UserModuleProgress",
  "type": "object",
  "properties": {
    "moduleId": {
      "type": "string"
    },
    "status": {
      "type": "string",
      "enum": [
        "Locked",
        "Available",
        "InProgress",
        "Complete"
      ],
      "default": "Locked"
    },
    "videoWatchedPercent": {
      "type": "number",
      "default": 0
    },
    "assessmentScore": {
      "type": "number"
    },
    "reflectionSubmitted": {
      "type": "boolean",
      "default": false
    },
    "completedAt": {
      "type": "string",
      "format": "date-time"
    },
    "description": {
      "type": "string",
      "maxLength": 1000
    }
  },
  "required": [
    "moduleId"
  ],
  "rls": {
    "create": {
      "created_by_id": "{{user.id}}"
    },
    "read": {
      "created_by_id": "{{user.id}}"
    },
    "update": {
      "created_by_id": "{{user.id}}"
    },
    "delete": {
      "created_by_id": "{{user.id}}"
    }
  }
}